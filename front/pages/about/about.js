require.config({
  baseUrl: "../../front",
  paths: {
    "jquery": "../bower_components/jquery/dist/jquery"
  }
});

require(['pages/about/about.section', function(section){ // relative to baseUrl
  //
}]);
