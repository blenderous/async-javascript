define(['jquery'], function(jquery) {
  console.log('slider code loaded!');

  var makeSlider = function (sliderData) {
    console.log('code to make the slider with data: ' + sliderData);
  };

  return { 'makeSlider' : makeSlider };
});
